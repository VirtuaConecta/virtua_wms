@model List<UsuarioViewModel>
@{
    ViewBag.Title = "Lista de usuários";
    Layout = "~/Views/Shared/__LayoutWms.cshtml";
}

@{
    var login = "";
}


@section MeusStyles
{
    <link href="~/assets/css/tabela_local.css" rel="stylesheet" />

    <style>
        .pm {
            cursor: pointer;
        }

        .detail {
            background-color: #d4d0d8;
            padding: 7px;
        }

  
    </style>
}

<div class="form-group">
    <h2 class="col-sm-11 ">Lista de Usuário</h2>
    @* <input class="col-sm-1 pm excel" style="height: 64px;width:64px;border:none" type="button" id="btnExport" />*@



    <a class="fa-4x fa fa-plus-circle" href="~/Cadastro/Cadastro_Usuario?Login=" id="CriarUser" style="border:none;background-color:transparent;"></a>
</div>

<div class="col-sm-12 col-md-12">
    <div class="block-flat">

        <table id="TabelaPed" class="tabelaFormato1">
            <thead>
                <tr>


                    <th>Nome</th>
                    <th>Login</th>

                    <th>Cargo</th>


                    <th>Status</th>
                    <th>Cpf</th>
                    <th></th>

                </tr>
            </thead>
            <tbody>

                @foreach (var user in Model)
                {
                    <tr>
                        <td>@user.NomeUsuario</td>
                        <td>@user.Login</td>

                        <td>@user.Cargo</td>
                        <td>@user.Bloqueado</td>
                        <td>@user.Cpf</td>
                        @{
                            login = "Cadastro_Usuario?Id_user="+ user.Codigo;
                        }

                        <td class="pm details-control" style="color:transparent;position:center;">
                            <a href="~/Cadastro/Cadastro_Usuario?Id_user=@user.Codigo"><img src="~/assets/img/edit_icon.png"/></a>
                         </td>

                    </tr>

                    }

            </tbody>
        </table>

    </div>

</div>

<div class="col-sm-12 col-md-12 modal" id="modal" style="background:transparent;">

</div>

@section MeusScripts{
    <script type="text/javascript">



        $(document).ready(function () {

            var tabela = $('#TabelaPed').DataTable({

                paging: true

            });



            //$('#TabelaPed tbody').on('click', 'td.details-control', function () {
            //    var oTr = $(this).parent('tr');
            //    var Login = oTr.find("td:eq(1)").html()
            //    $("#modal").load("Cadastro_Usuario?Login=" + Login, function () {
            //        $("#modal").modal();
            //    });
            //});

            //$('#CriarUser').on('click', function () {

            //    var Login = "";
            //    $("#modal").load("Cadastro_Usuario?Login=" + Login, function () {
            //        $("#modal").modal();
            //    });
            //});


        });

    </script>
}